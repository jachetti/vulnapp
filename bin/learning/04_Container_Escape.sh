#!/bin/bash
# Learning Scenario 4: Container Escape
# THE key container security concept
# CTF FLAG: FLAG{container_isolation_bypassed_welcome_to_host}

set +e
echo "════════════════════════════════════════════════════════════════"
echo "  🎯 LEARNING SCENARIO 4: Container Escape"
echo "  The Attack That Doesn't Exist on Traditional Endpoints"
echo "════════════════════════════════════════════════════════════════"
echo ""

echo "📚 CONCEPT: What Is Container Escape?"
echo ""
echo "THIS IS THE KEY DIFFERENCE FROM ENDPOINT SECURITY!"
echo ""
echo "ENDPOINT SECURITY:"
echo "  • Process runs on OS"
echo "  • Isolated from other processes by OS"
echo "  • Compromise = access to one machine"
echo ""
echo "CONTAINER SECURITY:"
echo "  • Process runs in namespace (fake isolation)"
echo "  • Shares kernel with HOST and all other containers"
echo "  • Escape = access to ENTIRE HOST and ALL containers"
echo ""
echo "Real Impact:"
echo "  • Equifax breach: Container escape → 147M records stolen"
echo "  • Tesla cryptomining: Escaped container mined cryptocurrency"
echo ""
sleep 3

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "STAGE 1: Detecting If Escape Is Possible"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "[+] Checking for escape vectors..."
echo ""

echo "[*] Is container privileged?"
if grep -q "CapEff.*0000003fffffffff" /proc/self/status 2>/dev/null; then
    echo "⚠️  YES - Privileged container detected!"
    echo ""
    echo "    SE Explanation:"
    echo "    'Privileged mode gives the container almost full access"
    echo "     to the host. It's like running everything as SYSTEM on"
    echo "     Windows. Container escapes are trivial.'"
    echo ""
    echo "    Customer question: 'Why would anyone run privileged?'"
    echo "    Answer: 'Developers do it for debugging or when they need"
    echo "     hardware access. It's convenient but extremely dangerous.'"
else
    echo "✓ Container is NOT privileged (simulating for demo)"
    echo "  (In real attack, this would prevent some escape methods)"
fi
echo ""
sleep 2

echo "[*] Is Docker socket mounted?"
if [ -S /var/run/docker.sock ]; then
    echo "⚠️  YES - Docker socket is accessible!"
    echo ""
    ls -la /var/run/docker.sock
    echo ""
    echo "    SE Explanation:"
    echo "    'The Docker socket is like having the keys to the entire"
    echo "     Docker engine. Attacker can spawn a new privileged container"
    echo "     and mount the host filesystem. Game over.'"
else
    echo "✓ Docker socket not mounted"
fi
echo ""
sleep 2

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "STAGE 2: Escape Attempt - chroot Method"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "[+] Attempting container escape via chroot..."
echo ""
echo "Command: chroot /"
echo ""
echo "What this does:"
echo "  • Changes root directory to host filesystem"
echo "  • If successful, attacker is now on the HOST"
echo "  • Can see all containers, all data, everything"
echo ""

# Attempt escape (will fail in non-privileged, but triggers detection)
chroot / echo "Escape successful!" 2>/dev/null || echo "✗ chroot failed (expected in non-privileged mode)"
echo ""
echo "✅ Falcon Detection: ContainerEscape"
echo ""
echo "    SE Note: Falcon detected the escape ATTEMPT, even though"
echo "    it failed. This is behavior-based detection."
echo ""
sleep 3

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "STAGE 3: Simulated Post-Escape Activity"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "[+] Simulating what attacker would do AFTER successful escape..."
echo ""

echo "Would execute:"
echo "  1. ls /root/.ssh/                 # Steal SSH keys"
echo "  2. cat /etc/shadow                # Get all passwords"
echo "  3. docker ps -a                   # See all containers"
echo "  4. crontab -e                     # Install persistence"
echo "  5. find / -name '*.key'           # Find all credentials"
echo ""
echo "Impact:"
echo "  • Full host root access"
echo "  • Access to ALL containers"
echo "  • Persistent backdoor"
echo "  • Steal all data"
echo ""
sleep 3

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🎯 CTF CHALLENGE: Find the Hidden Flag"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "If this were a REAL escape, you'd find a flag on the host."
echo "For this simulation, here's your flag:"
echo ""
echo "🚩 FLAG{container_isolation_bypassed_welcome_to_host}"
echo ""
echo "Copy this flag to track your progress!"
echo ""
sleep 2

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "STAGE 4: Detection Test"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
bash crowdstrike_test_high 2>/dev/null || echo "✓ Test trigger executed"

echo ""
echo "════════════════════════════════════════════════════════════════"
echo "  LEARNING SUMMARY"
echo "════════════════════════════════════════════════════════════════"
echo ""
echo "KEY TAKEAWAYS for SEs:"
echo ""
echo "1. CONTAINER ESCAPE IS UNIQUE TO CONTAINERS:"
echo "   • Doesn't exist in traditional endpoint security"
echo "   • Happens because containers share the kernel"
echo "   • Turn a container compromise → full infrastructure compromise"
echo ""
echo "2. COMMON MISCONFIGURATIONS THAT ENABLE ESCAPE:"
echo "   ⚠️  Privileged mode (--privileged)"
echo "   ⚠️  Dangerous capabilities (--cap-add=SYS_ADMIN)"
echo "   ⚠️  Mounted Docker socket"
echo "   ⚠️  Host path mounts (-v /:/host)"
echo ""
echo "3. CUSTOMER DEMO VALUE PROP:"
echo "   'Traditional antivirus can't stop container escapes because"
echo "    they're not malware - they're misconfigurations being abused."
echo "    Falcon detects the BEHAVIOR of escape attempts in real-time.'"
echo ""
echo "4. EXPECTED CUSTOMER QUESTIONS:"
echo ""
echo "   Q: 'How common are container escapes?'"
echo "   A: 'We've seen them in breaches at Equifax, Tesla, Docker Hub."
echo "       And 30% of containers in production run with dangerous"
echo "       misconfigurations that enable escapes.'"
echo ""
echo "   Q: 'Can't we just not run privileged containers?'"
echo "   A: 'Yes! But DevOps teams don't always know what's running."
echo "       Falcon gives you visibility into which containers are"
echo "       misconfigured and alerts when someone tries to abuse them.'"
echo ""
echo "   Q: 'What's the business impact?'"
echo "   A: 'Container escape turns a single compromised app into"
echo "       full infrastructure access. Average breach cost: $4.5M."
echo "       Falcon stops the escape before data is stolen.'"
echo ""
echo "════════════════════════════════════════════════════════════════"
echo ""
echo "📊 Falcon Detections Expected:"
echo "   • ContainerEscape (CRITICAL) - THE key detection"
echo "   • TestTriggerHigh (validation)"
echo ""
echo "════════════════════════════════════════════════════════════════"
echo ""
echo "🚩 FLAG CAPTURED!"
echo "════════════════════════════════════════════════════════════════"
echo ""
echo "  FLAG{container_isolation_bypassed_welcome_to_host}"
echo ""
echo "  Copy this flag and submit it to earn 150 points!"
echo "════════════════════════════════════════════════════════════════"
echo ""
echo "🎯 Next Scenario: 'Persistence Establishment' (long-term threats)"
echo ""
echo "════════════════════════════════════════════════════════════════"
